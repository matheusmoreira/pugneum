{
  "name": "pugneum-repository",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "jest": "^26.0.1",
    "prettier": "1.19.1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/matheusmoreira/pugneum"
  },
  "scripts": {
    "prettier:check": "prettier --ignore-path .gitignore --list-different './**/*.js'",
    "format": "prettier --ignore-path .gitignore --write './**/*.js'",
    "test": "jest"
  },
  "license": "MIT"
}
